for $prop, $val in merge($backgroundColor, $color)
    .{$c}\\\@text
        color: $val !important;

for $prop, $val in merge($backgroundColor, $color)
    .{$prop}\\\@border
        border-color: $val !important;

        &::before, &::after
            border-color: $val !important;

for $prop, $val in merge($backgroundColor, $color)
    .{$prop}\\\@bg
        background-color: $val !important;
