importThemeFile(getFilename(), $theme);
$borderColor = $colorBorder;
$borderRadius = $borderRadiusM;
$borderWidth = 1px;
$boxShadow = 0 1px 4px rgba(0, 0, 31, .1), 0 4px 14px rgba(0, 0, 31, .08);
$lineHeight = $lineHeightBase;
$padding = 1.2rem 0;
$itemPadding = .4rem 2rem;
$headerColor = $textColorBody;
$linkColor = $textColorBody;
$hoverLinkColor = $color.base;

.dropdown
    position: relative;
    display: inline-block;

    .toggle
        user-select: none;

    .menu
        absolute: top 3.2rem left 0;
        z-index: 800;
        padding: $padding;
        min-width: 21.6rem;
        border: $borderWidth solid $borderColor;
        border-radius: $borderRadius;
        background-color: $backgroundColor.white;
        box-shadow: $boxShadow;
        display: none;

        li, a
            display: block;
            line-height: $lineHeight;
            font-weight: $fontWeight.book;

        li
            padding: $itemPadding;

            &.header
                color: $headerColor;
                font-weight: $fontWeight.semiBold;
                font-size: $fontSize.body;

        a
            color: $linkColor;
            cursor: pointer;

            &:hover
                color: $hoverLinkColor;

    &.open
        .menu
            display: block;

    &.floating
        .menu
            margin-top: 1.6rem;

    &.right
        .menu
            right: 0;
            left: auto;

    &.upward
        .menu
            top: auto;
            bottom: 3.2rem;
