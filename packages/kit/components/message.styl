importThemeFile(getFilename(), $theme);
$borderColor = $colorBorder;
$borderRadius = $borderRadiusM;
$borderWidth = 6px;
$textColor = $textColorBody;
$lineHeight = $lineHeightBase;
$margin = 1.2rem 0;
$padding = 1.2rem 1.6rem;

styleColored(type)
    border-color: lookup('$colorBorder' + camelCase(type));
    background-color: lighten(@border-color, 90%);
    color: darken(@border-color, 45%);

    h5
        color: darken(@border-color, 60%);

.message
    margin: $margin;
    padding: $padding;
    border: 0;
    border-left: $borderWidth solid $backgroundColor.base;
    border-radius: $borderRadius;
    background-color: $backgroundColor.light;
    color: $textColor;
    line-height: $lineHeight;

    &.warning
        styleColored('warning');

    &.important
        styleColored('important');

    &.success
        styleColored('success');

    &.info
        styleColored('info');

    a
        color: inherit;
        text-decoration: underline;
