disabled()
    &.disabled, &[disabled]
        cursor: not-allowed;
        opacity: .6;
        pointer-events: none;

        {block};

inputPlaceholder()
    &::-webkit-input-placeholder
        {block};

    &:-moz-placeholder
        {block};

    &::-moz-placeholder
        {block};

    &:-ms-input-placeholder
        {block};
