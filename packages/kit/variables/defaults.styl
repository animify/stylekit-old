$borderRadius = {
    none: 0,
    small: 2px,
    base: 4px,
    medium: 6px,
    large: 8px,
    circle: 50%
};
$containerWidth ?= 980px;
$containerPadding ?= 3.2rem;
$sizeList ?= 'tiny' 'small' 'medium' 'large';
$listColors ?= 'red' 'orange' 'yellow' 'green' 'blue' 'purple' 'pink' 'black' 'base' 'white';
$variationColors ?= 12;
$breakpointIdentifiers ?= 't' 's' 'm' 'l';
$breakpointSizes ?= 0 480px 768px 1024px;
rupture.base-font-size = $fontSizeBody;
rupture.anti-overlap = true;
rupture.scale = $breakpointSizes;
rupture.scale-names = $breakpointIdentifiers;
