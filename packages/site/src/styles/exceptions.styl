.module .content
    .nav.menu
        position: static;
    .progress + .progress
        margin-top: 1rem;
    .button + .button, .label + .label
        margin-left: 1rem;

.menu
    .set
        display: inline;
    .set > .selecteditem
        relative: top .4rem left 2.4rem;
        font-weight: 500;
        color: $color.base;
        cursor: pointer;
        border-left: 2px solid $color.border;
        padding-left: 2.4rem;
        svg
            width: 1.6rem;
            height: 1.6rem;
            relative: top .2rem left .4rem;

.module
    .container
        background-color: #000;
        height: 120px;
        width: 320px;
        margin: 0 auto;
        border-radius: $borderRadiusM;
        &.fluid
            width: 100%
