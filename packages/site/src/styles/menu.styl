header
    .menu.sidebar
        display: flex;
        flex-direction: column;
        justify-content: center;
        .logo
            absolute: top left;
        .options
            display: flex;
            width: 100%
            align-self: center;
            flex-direction: column;
