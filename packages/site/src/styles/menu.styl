header
    .menu.basic
        background-color: $bgColorDark;
        fixed: top left;
        width: 100%;
        z-index: 1000;
        .item
            color: rgba(255, 255, 255, .7);
            position: relative;
            &:hover
                color: rgba(255, 255, 255, .85);
            &.active
                color: $colorWhite;
