header
    .menu
        .item
            position: relative;
        .minicon
            position: relative;
            top: 3px;
            height: 18px;
            width: 18px;
            margin-left: .3em;
