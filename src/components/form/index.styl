importThemeFile(getFilename(), $theme);
$backgroundColor = $color-white;
$borderColor = $color-border;
$focusBorderColor = $color-base;
$borderRadius = $border-radius-m;
$borderWidth = 1px;
$textCcolor = $color-text-body;
$disabledTextColor = $color-text-muted;
$boxShadow = $box-shadow-form;
$padding = 14px 18px;
$inputTransition = $animation-type-ease $animation-duration-m;

.input
    position: relative;
    font-style: normal;

    input[type='text'], input[type='number'], input[type='password'], textarea
        padding: $padding;
        min-width: 220px;
        outline: 0;
        border: 0;
        border: $borderWidth solid $borderColor;
        border-radius: $borderRadius;
        background-color: $backgroundColor;
        box-shadow: $boxShadow;
        color: $textCcolor;
        text-align: left;
        font-size: $font-size;
        font-family: $font-family-standard;
        line-height: $line-height;
        transition: border-color $inputTransition;

        &:focus
            border-color: $focusBorderColor;

        +inputPlaceholder()
            color: $disabledTextColor;

    &.transparent
        input[type='text'], input[type='number'], input[type='password'], textarea
            margin: 0;
            padding: 0;
            border: 0;
            background-color: transparent;

    +disabled()
        input[type='text'], input[type='number'], input[type='password'], textarea
            color: $disabledTextColor;

    textarea
        min-height: 8rem;
        max-height: 12rem;
        resize: vertical;
